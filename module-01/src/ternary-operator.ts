{
  const age = 15;
  if (age >= 18) {
    console.log("you are ready to biya");
  } else {
    console.log("tor beta biyar boyos hoi nai");
  }

  const isAdult = age >= 18 ? "adult" : "not adult";
  console.log(isAdult);
}
